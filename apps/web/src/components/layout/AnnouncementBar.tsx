// Announcement bar is now rendered inside Header.tsx as a thin black strip.
// This file is kept to avoid import errors in any existing references.

export default function AnnouncementBar() {
  return null;
}
